  高优先级问题 🔴

1. reagent_orders.py:298 - 导出时未处理 applicant_name

  writer.writerow([
      ...
      order.applicant_id or "",  # 导出的是ID而非姓名
      ...
  ])
  问题: 导出 CSV 时，申请人字段只导出了 applicant_id，而列表接口已经能返回
  applicant_name。导出的数据不直观，需要查询用户表获取姓名。
  建议: 在导出时同样查询并填充申请人姓名。**同意修改建议**

---

2. reagent_orders.py:213 - 总数查询效率问题

  total = db.exec(select(func.count()).select_from(base.subquery())).one()
  问题: 每次查询都使用子查询计算总数，当数据量大时会影响性能。库存页面已实现缓存，但试剂订单页面没有。
  建议: 考虑添加类似 inventory.py 的缓存机制。同意修改建议

---

3. inventory.py:129-163 - 内存缓存无清理机制

  SEARCH_CACHE: Dict[str, tuple[Any, datetime]] = {}
  问题: 缓存在多进程环境下无效（生产环境通常是 uvicorn
  多进程）。另外，缓存可能无限增长（虽然有100上限清理，但ttl检查在读取时，如果10秒内无人读取，缓存会保留）。
  建议: 考虑使用 Redis 或在后台定时清理过期缓存。暂时不修改

---

4. validationSchemas.ts:299 - 价格字段验证缺失

  export const ReagentOrderSchema = v.object({
    ...
    price: createPriceSchema(), // 必填
    ...
  })
  后端: reagent_order.py:56 定义 price: Optional[float]，前后端不一致！
  问题: 前端要求必填，后端定义为可选。
  建议: 统一前后端定义，要么都必填要么都可选。都必填

---

5. reagent_order.py:296 - ReagentOrderCreate.price 类型不一致

# ReagentOrder.py:56 (Model)

  price: Optional[float] = Field(None, ge=0)

# ReagentOrderCreate (line 96)

  price: Optional[float] = None  # 没有 ge=0 验证
  问题: 模型有非负约束，但 DTO 没有。输入验证是大于0的数字

---

  中等优先级问题 🟠

6. reagent_orders.py:242-246 - N+1 查询风险

  applicant_ids = {o.applicant_id for o in orders if o.applicant_id}
  users_map: dict[int, str] = {}
  if applicant_ids:
      users = db.exec(select(User).where(User.id.in_(applicant_ids))).all()
  问题: 这里已经做了优化（批量查询），但 inventory.py:765-796 的用户查询逻辑类似，可以复用为通用函数。 复用为通用函数

---

7. spec_utils.py:82 - 正则表达式不够严格

  pattern = r'^(\d+\.?\d*)\s*([a-zA-Zμ个瓶支盒包套]+)$'
  问题:

- 允许 1.5.5 这样的数字格式  修复
- μ (希腊字母 mu) 没有对应的单位映射
- 没有处理 L 的大写形式（只处理了小写） L输入大小写均可，因为后端清洗后保存全用小写，前端展示用大写L
  建议:

  pattern = r'^(\d+(?:\.\d+)?)\s*([a-zA-Zμ个瓶支盒包套]+)$'  

---

8. consumable_order.py:47 - specification 字段类型不一致

# Model

  specification: str = Field(max_length=100)

# DTO

  specification: str = Field(max_length=100)  # 必填
  问题: 与 reagent_order 的设计不一致。reagent_order 使用 initial_quantity + unit 存储，而 consumable_order 直接存储字符串。
  建议: 保持一致性，或者记录设计决策。consumable_order改为和reagent_order一致

---

9. frontend/src/api/client.ts:108-115 - API 参数类型定义不完整

  list: (params?: PaginationParams & {
      status_filter?: string
      search?: string
      ...
  }) => api.get('/reagent-orders/', { params }),
  问题: status_filter 应使用枚举类型而非 string，避免传入非法值导致后端错误。
  建议: 定义 ReagentOrderStatus 枚举并导出供前端使用。同意

---

10. BaseForm.tsx:215-224 - Autocomplete 未正确传递 readOnly

  {field.type === 'autocomplete' && (
    <Autocomplete
      ...
      className={getInputClassName(hasError, isReadOnly)}
    />
  )}
  问题: Autocomplete 组件没有 readOnly 属性传递，可能导致只读模式下仍可编辑。Autocomplete 组件不会只读

---

  低优先级问题 🟡

11. reagent_orders.py:39 - _add_specification 初始化值问题

  def _add_specification(item_dict: dict) -> dict:
      initial = item_dict.get("initial_quantity", 0)
      unit = item_dict.get("unit", "")
  问题: 当 initial_quantity 为 0 时，仍会返回 "0 mL" 格式。
  建议: 使用 None 处理空值情况。initial_quantity输入验证要大于0

---

12. inventory.py:705-711 - 未使用的导入

  from sqlmodel import case as sql_case
  代码中定义了 remaining_percent_expr 但未使用（已注释或未应用）。

---

13. validationSchemas.ts:302 - 未使用的 supplier 字段

  export const ReagentOrderSchema = v.object({
    ...
    supplier: v.optional(v.string()),  // 后端模型没有此字段！
    ...
  })
  问题: 前端验证包含 supplier 字段，但后端 ReagentOrderCreate 没有定义此字段。

---

  安全建议 🛡️

14. reagent_orders.py:261-308 - CSV 导出无权限检查

  @router.get("/export")
  def export_reagent_orders(
      db: Session = Depends(get_db),
      current_user: User = Depends(require_admin),  # 已有权限检查 ✓
  ):
  状态: ✅ 已有 require_admin 权限检查。
