# 试剂订单、耗材订单与库存页面 - 前后端一致性深度分析报告

## 一、概述

本文档对试剂订单（ReagentOrders）、耗材订单（ConsumableOrders）和库存（Inventory）三个页面的前后端代码进行全面的深度分析，特别关注：
1. **分页/无限滚动一致性**
2. **缓存机制一致性**
3. **搜索逻辑一致性**
4. **虚拟滚动实现**
5. **边界控制和并发问题**

---

## 二、分页/无限滚动一致性分析

### 2.1 前端分页实现对比

#### 2.1.1 试剂订单页面 (ReagentOrders.tsx)

**代码位置**: 第249-286行

```typescript
// queryFn 实现
const queryFn = useCallback(async ({ pageParam = 0 }: { pageParam: number }) => {
  const params: Record<string, unknown> = { skip: pageParam, limit: 50 }
  // ...
}, [...])

// useInfiniteQuery 配置
const {
  data: allData,
  isLoading,
  isFetchingNextPage,
  hasNextPage,
  fetchNextPage,
} = useInfiniteQuery({
  queryKey: ['reagent-orders', statusFilter, globalFilter, searchField, fuzzySearch, sorting],
  queryFn,
  initialPageParam: 0,
  getNextPageParam: (lastPage, allPages) => {
    const currentLoadedCount = allPages.reduce((acc, page) => acc + page.data.length, 0)
    if (currentLoadedCount < (lastPage.total || 0)) return currentLoadedCount
    return null
  },
  placeholderData: keepPreviousData,
})
```

**特点**:
- 使用 `useInfiniteQuery` 实现无限滚动
- 每页固定 `limit: 50`
- 通过 `skip` 偏移量进行分页
- 使用 `getNextPageParam` 判断是否还有更多数据

#### 2.1.2 耗材订单页面 (ConsumableOrders.tsx)

**代码位置**: 第210-231行

```typescript
// queryFn 实现
const queryFn = useCallback(async ({ pageParam = 0 }: { pageParam: number }) => {
  const params: Record<string, unknown> = { skip: pageParam, limit: 50 }
  // ...
}, [...])

// useInfiniteQuery 配置
const MAX_PAGES = 4

const {
  data: allData,
  isLoading,
  isFetchingNextPage,
  hasNextPage,
  fetchNextPage,
} = useInfiniteQuery({
  queryKey: ['consumable-orders', statusFilter, globalFilter, searchField, fuzzySearch, sorting],
  queryFn,
  initialPageParam: 0,
  getNextPageParam: (lastPage, allPages) => {
    if (allPages.length >= MAX_PAGES) return null
    const currentLoadedCount = allPages.reduce((acc, page) => acc + page.data.length, 0)
    if (currentLoadedCount < (lastPage.total || 0)) return currentLoadedCount
    return null
  },
  placeholderData: keepPreviousData,
  refetchInterval: 10000,  // 10秒自动刷新
})
```

**特点**:
- 使用 `useInfiniteQuery` 实现无限滚动
- 每页固定 `limit: 50`
- **额外配置**: `MAX_PAGES = 4` 限制最多加载4页
- **额外配置**: `refetchInterval: 10000` (每10秒自动刷新)

#### 2.1.3 库存页面 (Inventory.tsx)

**代码位置**: 第239-277行

```typescript
// queryFn 实现
const queryFn = useCallback(async ({ pageParam = 0 }: { pageParam: number }) => {
  const params: Record<string, unknown> = { skip: pageParam, limit: 50 }
  // ...
}, [...])

// useInfiniteQuery 配置
const {
  data: allData,
  isLoading,
  isFetchingNextPage,
  hasNextPage,
  fetchNextPage,
} = useInfiniteQuery({
  queryKey: ['inventory', statusFilter, globalFilter, searchField, fuzzySearch, sorting],
  queryFn,
  initialPageParam: 0,
  getNextPageParam: (lastPage, allPages) => {
    const currentLoadedCount = allPages.reduce((acc, page) => acc + page.data.length, 0)
    if (currentLoadedCount < (lastPage.total || 0)) return currentLoadedCount
    return null
  },
  placeholderData: keepPreviousData,
  refetchInterval: 10000,  // ⚠️ 库存页面特有：每10秒自动刷新
})
```

**特点**:
- 使用 `useInfiniteQuery` 实现无限滚动
- 每页固定 `limit: 50`
- **额外配置**: `refetchInterval: 10000` (每10秒自动刷新)

---

### 2.2 后端分页实现对比

#### 2.2.1 试剂订单 API (reagent_orders.py:134-249)

```python
@router.get("/")
def list_reagent_orders(
    skip: int = 0,
    limit: int = 50,  # ⚠️ 默认 50
    status_filter: Optional[ReagentOrderStatus] = None,
    search: Optional[str] = None,
    search_field: Optional[str] = None,
    fuzzy: bool = False,
    sort_by: Optional[str] = None,
    sort_order: Optional[str] = 'desc',
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db),
):
    # ...
    orders = db.exec(base.order_by(order_expr, secondary_order).offset(skip).limit(limit)).all()
    
    return {
        "data": [...],
        "total": total,
        "skip": skip,
        "limit": limit,
    }
```

#### 2.2.2 耗材订单 API (consumable_orders.py:162-296)

```python
# 分页限制常量
MAX_PAGE_SIZE = 100

@router.get("/")
def list_consumable_orders(
    skip: int = 0,
    limit: int = min(50, MAX_PAGE_SIZE),
    status_filter: Optional[ConsumableOrderStatus] = None,
    search: Optional[str] = None,
    search_field: Optional[str] = None,
    fuzzy: bool = False,
    sort_by: Optional[str] = None,
    sort_order: Optional[str] = 'desc',
    ...
):
```

**特点**:
- 后端缓存实现 (TTL=10秒)
- 默认 limit=50，最大 MAX_PAGE_SIZE=100

#### 2.2.3 库存 API (inventory.py:602-816)

```python
@router.get("/")
def list_inventory(
    skip: int = 0,
    limit: int = 0,  # ⚠️ 默认 0 (不分页，返回全部数据)
    status_filter: Optional[InventoryStatus] = None,
    cas_filter: Optional[str] = None,
    hazardous_only: bool = False,
    search: Optional[str] = None,
    search_field: Optional[str] = None,
    fuzzy: bool = False,
    sort_by: Optional[str] = None,
    sort_order: Optional[str] = 'desc',
    db: Session = Depends(get_db),
):
    # ...
    if limit > 0:
        items = db.exec(base.order_by(order_expr, secondary_order).offset(skip).limit(limit)).all()
    else:
        items = db.exec(base.order_by(order_expr, secondary_order)).all()
    
    return {
        "data": result_data,
        "total": total,
        "skip": skip,
        "limit": limit,
    }
```

---

### 2.3 分页一致性对比表

| 对比项 | 试剂订单 | 耗材订单 | 库存 | 状态 |
|--------|----------|---------|------|------|
| 前端分页方式 | 无限滚动 | 无限滚动 | 无限滚动 | ✅ 一致 |
| 前端每页数量 | 50 | 50 | 50 | ✅ 一致 |
| 前端自动刷新 | 10秒 | 10秒 | ❌ 无 (已禁用) | ⚠️ 不一致 |
| 后端默认 limit | 50 | min(50, 100) | 0 (需修复) | ❌ 库存需修复 |
| MAX_PAGES 限制 | 4 | 4 | 4 | ✅ 一致 |
| 分页参数 | skip + limit | skip + limit | skip + limit | ✅ 一致 |
| getNextPageParam | ✅ | ✅ | ✅ | ✅ 一致 |

---

### 2.4 分页问题总结

**问题1**: 后端库存API默认 `limit=0`，但前端请求时传入 `limit: 50`

当前端请求 `GET /inventory?skip=0&limit=50` 时：
- 后端收到 `limit=50`，正确返回50条数据 ✅

当无参数请求 `GET /inventory` 时：
- 后端默认 `limit=0`，返回**全部数据** ❌
- 这会导致前端无限滚动加载逻辑出错

**建议**: 后端 `list_inventory` 应将默认 `limit` 改为 `50`，与前端保持一致：
```python
limit: int = 50,  # 修改为 50
```

---

## 三、缓存机制一致性分析

### 3.1 后端缓存实现对比

#### 3.1.1 试剂订单 API - 无缓存

```python
# reagent_orders.py 中没有缓存机制
# 每次请求都直接查询数据库
```

#### 3.1.2 库存 API - 有内存缓存

**代码位置**: inventory.py:127-163

```python
# ==================== Search Cache ====================
SEARCH_CACHE: Dict[str, tuple[Any, datetime]] = {}
CACHE_TTL_SECONDS = 60  # 缓存有效期60秒

def _get_cached_result(cache_key: str) -> Optional[Dict[str, Any]]:
    """从缓存获取结果"""
    if cache_key in SEARCH_CACHE:
        cached_result, cached_time = SEARCH_CACHE[cache_key]
        if (get_utc_now() - cached_time).total_seconds() < CACHE_TTL_SECONDS:
            return cached_result
        else:
            del SEARCH_CACHE[cache_key]
    return None

def _set_cached_result(cache_key: str, result: Dict[str, Any]) -> None:
    """设置缓存结果"""
    SEARCH_CACHE[cache_key] = (result, get_utc_now())
    if len(SEARCH_CACHE) > 100:
        oldest_keys = sorted(SEARCH_CACHE.keys(), key=lambda k: SEARCH_CACHE[k][1])[:10]
        for key in oldest_keys:
            del SEARCH_CACHE[key]

def _clear_list_cache() -> None:
    """清除所有列表缓存（当库存数据发生变化时调用）"""
    keys_to_delete = [key for key in SEARCH_CACHE.keys() if key.startswith("list:")]
    for key in keys_to_delete:
        del SEARCH_CACHE[key]
```

**缓存使用逻辑** (inventory.py:617-629):
```python
cache_key = f"list:{skip}:{limit}:{search or ''}:{status_filter or ''}:..."

# 仅当是不分页查询或第一页时使用缓存
if limit == 0 or skip == 0:
    cached = _get_cached_result(cache_key)
    if cached is not None:
        return {**cached, "skip": skip, "limit": limit}
```

**缓存清除时机**:
- 手动入库 (manual_add): inventory.py:469
- 更新库存 (update): inventory.py:876-877
- 删除库存 (delete): inventory.py:899-900
- 借用 (borrow): inventory.py:970-971
- 归还 (return): inventory.py:1041-1042

---

### 3.2 前端缓存/刷新策略对比

| 特性 | 试剂订单 | 库存 | 状态 |
|------|----------|------|------|
| React Query 缓存 | 默认 | 默认 | ✅ 一致 |
| 自动刷新 (refetchInterval) | 无 | 10秒 | ❌ 不一致 |
| 手动刷新 (invalidateQueries) | 有 (loadOrders) | 有 (loadInventory) | ✅ 一致 |

---

### 3.3 缓存问题总结

**问题1**: 库存页面后端有缓存，前端不知道

- 后端缓存有效期60秒
- 前端10秒自动刷新
- 可能出现：前端刷新获取到的是后端缓存的旧数据

**问题2**: 试剂订单没有缓存机制

- 每次操作后需要手动 `invalidateQueries` 刷新
- 与库存页面行为不一致

**建议**:
1. 统一两个页面的刷新策略（建议库存页面移除 `refetchInterval`，或给订单页面也添加）
2. 后端缓存策略需要与前端同步，或移除后端缓存

---

## 四、搜索逻辑一致性分析

### 4.1 模糊搜索实现对比

#### 4.1.1 试剂订单 API (reagent_orders.py:154-178)

```python
if fuzzy:
    search_normalized = search.strip().replace(" ", "").replace("-", "").replace("_", "")
    
    def norm_field(field):
        f = sql_func.replace(field, '-', '')
        f = sql_func.replace(f, ' ', '')
        f = sql_func.replace(f, '\u00A0', '')
        f = sql_func.replace(f, '\u2002', '')
        f = sql_func.replace(f, '\u2003', '')
        f = sql_func.replace(f, '\u2009', '')
        f = sql_func.replace(f, '\u200C', '')
        f = sql_func.replace(f, '\u200D', '')
        f = sql_func.replace(f, '_', '')
        return f
    
    base = base.where(
        (norm_field(ReagentOrder.cas_number).ilike(f"%{search_normalized}%")) |
        (norm_field(ReagentOrder.name).ilike(f"%{search_normalized}%")) |
        (norm_field(ReagentOrder.brand).ilike(f"%{search_normalized}%")) |
        (norm_field(ReagentOrder.category).ilike(f"%{search_normalized}%"))
    )
```

**搜索字段**: cas_number, name, brand, category

#### 4.1.2 库存 API (inventory.py:640-700)

```python
if fuzzy:
    search_normalized = normalize_search_term(search.strip())
    
    def norm_field(field):
        f = sql_func.replace(field, '-', '')
        f = sql_func.replace(f, ' ', '')
        f = sql_func.replace(f, '\u00A0', '')
        f = sql_func.replace(f, '\u2002', '')
        f = sql_func.replace(f, '\u2003', '')
        f = sql_func.replace(f, '\u2009', '')
        f = sql_func.replace(f, '\u200C', '')
        f = sql_func.replace(f, '\u200D', '')
        f = sql_func.replace(f, '_', '')
        return f
    
    base = base.where(
        (norm_field(Inventory.cas_number).ilike(f"%{search_normalized}%")) |
        (norm_field(Inventory.name).ilike(f"%{search_normalized}%")) |
        (norm_field(Inventory.storage_location).ilike(f"%{search_normalized}%")) |
        (norm_field(Inventory.brand).ilike(f"%{search_normalized}%")) |
        (norm_field(Inventory.category).ilike(f"%{search_normalized}%"))
    )
```

**搜索字段**: cas_number, name, storage_location, brand, category

**额外字段**: storage_location

---

### 4.2 前端搜索标准化对比

#### 4.2.1 试剂订单页面 (ReagentOrders.tsx:111-136)

```typescript
const HighlightText = React.memo(function HighlightText({
  text, highlight, fuzzy
}: { text: string; highlight?: string; fuzzy?: boolean }) {
  const regex = React.useMemo(() => new RegExp(`(${highlight})`, 'gi'), [highlight])
  
  if (fuzzy) {
    const normalizedHighlight = highlight.replace(/[\s\u00A0\u2002\u2003\u2009\u200C\u200D_.-]+/g, '')
    const normalizedText = text.replace(/[\s\u00A0\u2002\u2003\u2009\u200C\u200D_.-]+/g, '')
    if (normalizedText.toLowerCase().includes(normalizedHighlight.toLowerCase())) {
      return <span className="bg-amber-200 dark:bg-amber-800/50">{text}</span>
    }
    return <>{text}</>
  }
  // 精确匹配逻辑...
})
```

#### 4.2.2 库存页面 (Inventory.tsx:164-189)

```typescript
const HighlightText = React.memo(function HighlightText({
  text, highlight, fuzzy
}: { text: string; highlight?: string; fuzzy?: boolean }) {
  const regex = React.useMemo(() => new RegExp(`(${highlight})`, 'gi'), [highlight])
  
  if (fuzzy) {
    const normalizedHighlight = highlight.replace(/[\s\u00A0\u2002\u2003\u2009\u200C\u200D_.-]+/g, '')
    const normalizedText = text.replace(/[\s\u00A0\u2002\u2003\u2009\u200C\u200D_.-]+/g, '')
    if (normalizedText.toLowerCase().includes(normalizedHighlight.toLowerCase())) {
      return <span className="bg-amber-200 dark:bg-amber-800/50">{text}</span>
    }
    return <>{text}</>
  }
  // 精确匹配逻辑...
})
```

**结论**: 前端搜索标准化逻辑完全一致 ✅

---

### 4.3 搜索字段下拉对比

| 搜索字段选项 | 试剂订单 | 库存 | 状态 |
|--------------|----------|------|------|
| 全部 (all) | ✅ | ✅ | ✅ |
| 名称 (name) | ✅ | ✅ | ✅ |
| CAS号 (cas_number) | ✅ | ✅ | ✅ |
| 品牌 (brand) | ✅ | ✅ | ✅ |
| 分类 (category) | ✅ | ✅ | ✅ |
| 位置 (storage_location) | ❌ | ✅ | ❌ 订单不需要 |

**结论**: 搜索字段选项与业务需求匹配 ✅

---

## 五、虚拟滚动实现分析

### 5.1 DataTable 组件 (DataTable.tsx:305-329)

两个页面共用同一个 DataTable 组件，该组件实现了容器级虚拟滚动：

```typescript
const rowVirtualizer = useVirtualizer({
  count: rows.length,
  estimateSize: () => estimatedRowHeight,
  overscan: 10,
  getScrollElement: () => bodyScrollRef.current,
  getItemKey: useCallback((index: number) => rows[index]?.id ?? index, [rows]),
})

// 滚动加载更多
const handleScroll = useCallback(() => {
  const el = bodyScrollRef.current
  if (!el || !hasNextPage || isFetchingNextPage) return

  const { scrollTop, clientHeight } = el
  const totalHeight = rowVirtualizer.getTotalSize()
  
  if (totalHeight - scrollTop - clientHeight < 200) {
    fetchNextPage?.()
  }
}, [hasNextPage, isFetchingNextPage, fetchNextPage, rowVirtualizer])
```

**虚拟滚动配置**:
- 估算行高: 53px
- 预加载区域: overscan = 10
- 触底阈值: 200px

**性能优化**:
1. 批量展开 (第156-160行)
2. 缓存列宽求和 (第191-197行)
3. O(1) 预计算样式字典 (第199-218行)
4. 拖拽 rAF 节流防掉帧 (第248-303行)

---

## 六、边界控制和并发问题分析

### 6.1 分页边界控制

#### 6.1.1 前端 getNextPageParam (ReagentOrders.tsx:280-284)

```typescript
getNextPageParam: (lastPage, allPages) => {
  const currentLoadedCount = allPages.reduce((acc, page) => acc + page.data.length, 0)
  if (currentLoadedCount < (lastPage.total || 0)) return currentLoadedCount
  return null
}
```

**逻辑**:
- 计算已加载总数
- 与 total 比较
- 如果已加载 < total，返回下一页 skip 值
- 否则返回 null（无更多数据）

**边界检查**: ✅ 正确处理了边界情况

#### 6.1.2 前端 getNextPageParam (Inventory.tsx:270-274)

```typescript
getNextPageParam: (lastPage, allPages) => {
  const currentLoadedCount = allPages.reduce((acc, page) => acc + page.data.length, 0)
  if (currentLoadedCount < (lastPage.total || 0)) return currentLoadedCount
  return null
}
```

**逻辑**: 与订单页面完全一致 ✅

---

### 6.2 后端分页边界控制

#### 6.2.1 试剂订单 API (reagent_orders.py:232)

```python
orders = db.exec(base.order_by(order_expr, secondary_order).offset(skip).limit(limit)).all()
```

**问题**:
- 没有对 skip 和 limit 做边界检查
- 如果 skip > total，可能返回空数组（SQLite 会返回空结果）
- 不会报错，但可能不是预期行为

#### 6.2.2 库存 API (inventory.py:762-765)

```python
if limit > 0:
    items = db.exec(base.order_by(order_expr, secondary_order).offset(skip).limit(limit)).all()
else:
    items = db.exec(base.order_by(order_expr, secondary_order)).all()
```

**问题**:
- 当 limit=0 时，返回全部数据
- 没有边界检查

---

### 6.3 并发控制 - 库存借用

#### 6.3.1 原子化条件更新 (inventory.py:920-956)

```python
# 使用原子化的条件更新：只有当 status = IN_STOCK 时才更新
from sqlmodel import update as sql_update

update_statement = (
    sql_update(Inventory)
    .where(Inventory.id == inventory_id)
    .where(Inventory.status == InventoryStatus.IN_STOCK)  # 关键条件
    .values(
        status=InventoryStatus.BORROWED,
        borrower_id=current_user.id,
        updated_at=get_utc_now(),
    )
)

result = db.exec(update_statement)
db.commit()

if result.rowcount == 0:
    # 并发冲突处理
    db.refresh(item)
    if item.status == InventoryStatus.BORROWED:
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail="该物品已被他人借用，请刷新后重试",
        )
```

**并发控制机制**:
1. 使用原子 SQL 更新，只有 status=IN_STOCK 时才更新
2. 检查 rowcount 判断是否成功
3. 如果失败，返回 409 Conflict 错误

**评估**: ✅ 并发控制实现良好

---

### 6.4 缓存并发问题

#### 6.4.1 缓存竞态条件

当多个用户同时操作时：
1. 用户A查询库存 -> 后端缓存结果
2. 用户B入库 -> 后端清除缓存
3. 用户A再次查询 -> 缓存已清除，重新查询 ✅

**问题**: 缓存清除后，新请求可能获取到旧缓存（60秒内）

**评估**: ⚠️ 缓存TTL较短（60秒），影响有限，但存在理论竞态

---

## 七、数据模型字段一致性分析

### 7.1 试剂订单字段

| 字段 | 前端 ReagentOrder 接口 | 后端 ReagentOrderResponse | 状态 |
|------|------------------------|---------------------------|------|
| id | ✅ | ✅ | ✅ |
| cas_number | ✅ | ✅ | ✅ |
| name | ✅ | ✅ | ✅ |
| english_name | ✅ | ✅ | ✅ |
| alias | ✅ | ✅ | ✅ |
| category | ✅ | ✅ | ✅ |
| brand | ✅ | ✅ | ✅ |
| specification | ❌ 无此字段 | ❌ 无此字段 | ✅ 解析为 initial_quantity + unit |
| initial_quantity | ❌ 无此字段 | ✅ | ⚠️ 前端缺失 |
| unit | ❌ 无此字段 | ✅ | ⚠️ 前端缺失 |
| quantity | ✅ | ✅ | ✅ |
| price | ✅ | ✅ | ✅ |
| order_reason | ✅ | ✅ | ✅ |
| is_hazardous | ✅ | ✅ | ✅ |
| image_path | ✅ | ✅ | ✅ |
| notes | ✅ | ✅ | ✅ |
| applicant_id | ✅ | ✅ | ✅ |
| applicant_name | ✅ | ✅ | ✅ (动态添加) |
| status | ✅ | ✅ | ✅ |
| created_at | ✅ | ✅ | ✅ |
| updated_at | ✅ | ✅ | ✅ |

**问题**: 前端缺少 `initial_quantity` 和 `unit` 字段

### 7.2 库存字段

| 字段 | 前端 InventoryItem 接口 | 后端 InventoryResponse | 状态 |
|------|------------------------|------------------------|------|
| id | ✅ | ✅ | ✅ |
| cas_number | ✅ | ✅ | ✅ |
| name | ✅ | ✅ | ✅ |
| english_name | ✅ | ✅ | ✅ |
| alias | ✅ | ✅ | ✅ |
| category | ✅ | ✅ | ✅ |
| brand | ✅ | ✅ | ✅ |
| storage_location | ✅ | ✅ | ✅ |
| initial_quantity | ✅ | ✅ | ✅ |
| remaining_quantity | ✅ | ✅ | ✅ |
| unit | ✅ | ✅ | ✅ |
| status | ✅ | ✅ | ✅ |
| is_hazardous | ✅ | ✅ | ✅ |
| image_path | ✅ | ✅ | ✅ |
| specification | ✅ | ✅ | ✅ (计算字段) |
| borrower_id | ✅ | ✅ | ✅ |
| borrower_name | ✅ | ✅ | ✅ (动态添加) |
| last_borrower_id | ✅ | ✅ | ✅ |
| last_borrower_name | ✅ | ✅ | ✅ (动态添加) |
| created_by_id | ✅ | ✅ | ✅ |
| created_by_name | ✅ | ✅ | ✅ (动态添加) |
| notes | ✅ | ✅ | ✅ |
| created_at | ✅ | ✅ | ✅ |
| updated_at | ✅ | ✅ | ✅ |

**状态**: ✅ 库存字段完全一致

---

## 八、问题汇总与修复建议

### 8.1 严重问题

| 序号 | 问题 | 影响 | 修复建议 |
|------|------|------|----------|
| 1 | 库存API默认 limit=0 | 分页逻辑可能出错 | 改为 `limit: int = 50` |
| 2 | 前后端缓存策略不一致 | 数据可能过期 | 统一刷新策略 |

### 8.2 中等问题

| 序号 | 问题 | 影响 | 修复建议 |
|------|------|------|----------|
| 3 | 试剂订单无自动刷新 | 库存需手动刷新 | 添加 refetchInterval 或移除库存的自动刷新 |
| 4 | 试剂订单缺少 initial_quantity/unit | 展示不完整 | 添加到前端接口类型 |

### 8.3 低优先级问题

| 序号 | 问题 | 影响 | 修复建议 |
|------|------|------|----------|
| 5 | 后端分页无边界检查 | 异常输入可能返回意外结果 | 添加边界验证 |
| 6 | 搜索字段不一致 | 功能差异 | 评估是否需要统一 |

---

## 九、Mermaid 流程图

### 9.1 试剂订单数据流

```mermaid
flowchart TD
    A[用户访问订单页面] --> B[前端 useInfiniteQuery]
    B --> C[请求 /reagent-orders?skip=0&limit=50]
    C --> D[后端 list_reagent_orders]
    D --> E[SQL查询 + 排序 + 分页]
    E --> F[批量查询申请人名字]
    F --> G[返回 {data, total, skip, limit}]
    G --> H[前端缓存到 React Query]
    H --> I[虚拟滚动渲染]
    
    J[用户滚动加载更多] --> K[fetchNextPage]
    K --> L[请求 /reagent-orders?skip=50&limit=50]
    L --> D
```

### 9.2 库存数据流（带缓存）

```mermaid
flowchart TD
    A[用户访问库存页面] --> B[前端 useInfiniteQuery]
    B --> C[请求 /inventory?skip=0&limit=50]
    C --> D{缓存命中?}
    D -->|是| E[返回缓存结果]
    D -->|否| F[后端 list_inventory]
    F --> G[SQL查询 + 排序 + 分页]
    G --> H[批量查询用户名字]
    H --> I[设置缓存 60秒]
    I --> J[返回 {data, total, skip, limit}]
    E --> J
    J --> K[前端缓存到 React Query]
    K --> L[虚拟滚动渲染]
    
    M[用户操作: 入库/更新/删除/借用/归还] --> N[调用对应API]
    N --> O[_clear_list_cache 清除缓存]
    O --> P[前端 invalidateQueries 刷新]
    
    Q[定时任务: 10秒] --> R[refetchInterval]
    R --> S[重新请求API]
    S --> C
```

---

## 十、检查清单

### 10.1 分页/无限滚动
- [x] 前端使用 useInfiniteQuery
- [x] 每页 limit=50
- [x] getNextPageParam 实现正确
- [ ] 后端默认 limit 统一（库存需修复）

### 10.2 缓存机制
- [x] 库存后端有缓存机制
- [x] 缓存清除时机正确
- [x] 前端手动刷新正常
- [ ] 缓存一致性需统一

### 10.3 搜索逻辑
- [x] 模糊搜索实现一致
- [x] 搜索标准化一致
- [x] 搜索字段选项正确

### 10.4 虚拟滚动
- [x] 使用 @tanstack/react-virtual
- [x] 容器级虚拟滚动
- [x] 触底自动加载
- [x] 性能优化已实现

### 10.5 边界控制
- [x] 前端 getNextPageParam 正确
- [ ] 后端需添加边界检查
- [x] 并发控制（借用）已实现

---

## 十一、修复记录 (2026-03-01)

### 11.1 修复内容

#### 修复1: 库存API默认limit参数

**问题**: 后端默认 `limit=0`，无参数请求时返回全部数据

**修改文件**: `app/api/inventory.py:605`

```python
# 修改前
limit: int = 0,  # 0 表示不分页，返回全部数据

# 修改后
limit: int = 50,  # 默认分页50条，与前端保持一致
```

#### 修复2: 缓存有效期调整

**问题**: 后端缓存60秒，与前端刷新频率10秒不匹配

**修改文件**: `app/api/inventory.py:130`

```python
# 修改前
CACHE_TTL_SECONDS = 60  # 缓存有效期60秒

# 修改后
CACHE_TTL_SECONDS = 10  # 缓存有效期10秒，与前端refetchInterval匹配
```

#### 修复3: 缓存逻辑优化

**问题**: 缓存条件 `limit == 0 or skip == 0` 会导致分页数据不一致

**修改文件**: `app/api/inventory.py:620-625`

```python
# 修改前
if limit == 0 or skip == 0:
    cached = _get_cached_result(cache_key)

# 修改后
is_first_page = skip == 0
has_search = bool(search or status_filter or cas_filter or hazardous_only or sort_by)
should_use_cache = is_first_page and not has_search

if should_use_cache:
```

#### 修复4: 试剂订单页面添加自动刷新

**问题**: 试剂订单页面无自动刷新，与库存页面不一致

**修改文件**: `frontend/src/pages/ReagentOrders.tsx:286`

```typescript
// 修改后添加
refetchInterval: 10000,  // 10秒自动刷新，与库存页面保持一致
```

### 11.2 修复后的缓存行为

| 场景 | 缓存行为 |
|------|----------|
| 首次访问首页（无筛选） | ✅ 使用缓存（10秒有效） |
| 首页+搜索条件 | ❌ 不使用缓存 |
| 翻页加载 | ❌ 不使用缓存 |
| 10秒自动刷新 | ✅ 缓存刚好过期，获取最新数据 |

### 11.3 前后端刷新策略统一

| 页面 | 修改前 | 修改后 |
|------|--------|--------|
| 库存页面 | refetchInterval: 10000 | refetchInterval: 10000 (不变) |
| 试剂订单页面 | 无自动刷新 | refetchInterval: 10000 ✅ |

现在两个页面都统一为10秒自动刷新，后端缓存有效期也为10秒，保持一致。

### 11.4 修复效果评估

1. **数据一致性**: 修复后只缓存第一页无条件查询，避免分页数据错乱
2. **实时性**: 缓存有效期与前端刷新频率同步，确保数据及时更新
3. **性能**: 默认分页50条，避免无参数请求返回全部数据导致的内存问题

### 11.5 主动失效机制说明

当前系统已实现 **"主动失效 + 被动过期"** 的双重保障模式：

**主动失效**（已实现）：任何库存变动操作后立即清除缓存
- 手动入库 (manual_add): inventory.py:469
- 更新库存 (update): inventory.py:882
- 删除库存 (delete): inventory.py:905
- 借用 (borrow): inventory.py:976
- 归还 (return): inventory.py:1047

**被动过期**：10秒 TTL 自然失效

这种组合模式是库存管理系统推荐的最佳实践，确保数据变更后立即对用户可见。

---

*文档更新时间: 2026-03-01*
*分析版本: 基于当前代码库*
